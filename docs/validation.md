# 0.2 开发里程碑验证

日期：2026-09-14。Windows / Node 24.19.0 / pnpm 11.19.0 / Chromium 153；所有自动化使用测试数据。

## 本次验证

- `pnpm test`：26 项通过，新增旧 IndexedDB v1 和 JSON 备份迁移、同步基准深拷贝隔离、三方记录合并、同记录编辑/删除冲突、跨记录退款超额保护、重复实例稳定 ID、月末锚点、单次/后续修改、完成恢复历史、工作日与截至日期、倒计时历史、提醒筛选、设备令牌隔离与撤销、原生来源 CORS。
- `pnpm build`：TypeScript 和生产构建通过，PWA 缓存保留。
- `pnpm test:e2e`：4 条完整浏览器场景通过。新增日历点击创建、真实鼠标跨日期拖拽（跨越 1 秒计时刷新）、日周月切换、手机七列日历完整显示、重复完成生成下次、倒计时完成历史与重载。双设备场景验证独立修改自动合并、共同记录逐条选择且无关记录保留。
- Windows NSIS 与 Android Debug APK 已在 GitHub Actions 构建成功。目标机安装与通知可靠性需单独记录，不能把构建通过写作实机通过。
- 检查 1440 px 桌面和 390 px 手机日历截图，并继续验证六个原有页面无横向溢出。发现的“计时刷新打断日历拖拽”和“同步基准共享对象导致新记录丢失”已修复并加入回归覆盖。

## 未通过全量验收的部分

- vivo X300 Pro 安装/通知授权/省电/锁屏/重启/强停；真实 Android 与 Windows 两端的断网恢复和生产 HTTPS 同步。
- 通知快捷操作、免打扰、正式发布签名、长期运行与大数据量。
- 应用层本地加密、账号注销、密钥轮换与找回、远端历史/备份的永久清除流程。

目标机步骤与安装说明见 [native.md](native.md)。以下保留 0.1 的历史验证，不能据此推定 0.2 原生验收。

---

# 0.1 开发里程碑验证

日期：2026-09-14。环境：Windows、Node 24.19.0、pnpm 11.19.0、Chromium 153。使用测试数据和内存同步服务器，无真实个人数据或第三方账号。

## 自动化验证

- `pnpm test`：15 项通过。覆盖整数金额解析、月末/闰年周期、转账/待确认排除、跨月退款与超额退款、关联删除、会员历史与账单原子变更、习惯状态和连续段、导入校验、暂停计时、本机并发写入与回滚、加密/篡改检测、同步鉴权及并发 CAS。
- `pnpm build`：TypeScript 检查与 Vite 生产构建通过，生成离线 Service Worker 与 PWA manifest。
- `pnpm test:e2e`：3 条完整场景通过。真实浏览器验证任务添加/完成/刷新、习惯打卡、手动记账、超额退款拦截、会员续费生成账单、回收站恢复、断网后保存完成再刷新、手机六页面无横向溢出；独立设备同步、显式冲突及历史备份；同源多标签页金额编辑冲突与 JSON 导出/恢复。

## 视觉检查

已检查 1440 px 桌面今天页，及 390 px 手机今天、计划、习惯、账本、会员、设置页。导航和内容适配，表单使用原生对话框焦点约束；核心输入框有明确可访问名称。截图位于 `artifacts/`，其中出现的任务和账单均为测试数据，不是默认种子数据。

## 尚未验证 / 尚未交付

- vivo X300 Pro 实机、Android APK 与 Windows 原生安装包。
- 系统通知、后台运行/重启后的提醒可靠性。
- 生产 HTTPS 反向代理、长时间运行、大数据量、真实网络故障恢复与密钥轮换。
- 本地应用层加密、用户账号生命周期及远端备份永久清除。
- P0 完整日周月日历、重复任务、倒计时、单记录同步冲突。

本次完成的是可运行开发版本，不能作为 PRD 的全部 P0 上线验收。运行时允许用户使用本地模式，并明确显示未接入能力。
