import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = {
  appId: "com.self.steward",
  appName: "自明 SELF",
  webDir: "dist",
  android: { allowMixedContent: false },
};
export default config;
